package programmers;

public class 최댓값 {

	public int solution(int[] numbers) {

		int maxProduct = numbers[0];
		int secondProduct =numbers[0];

		for (int i = 1; i < numbers.length; i++) {
			maxProduct = Math.max(maxProduct, numbers[i]);
			secondProduct=
			
		}

		return maxProduct;
	}

}
